import '../styles/schedule.css';
import '../styles/shared.css';

const Schedule = (): JSX.Element => {
    return (
        <div>
            Schedule
        </div>
    );
}

export default Schedule;